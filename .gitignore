# Build output
/ps2hdd
/ps2hdd-linux-*
/dist/

# Local scratch
*.img
*.vcd
*.partial

# hdl_dump writes an info.sys into the working directory when it installs a
# game with HDD OSD attributes. It is drive state, not source.
/info.sys
